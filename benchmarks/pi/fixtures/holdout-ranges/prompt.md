Complete compactRanges(ranges) in ranges.mjs and totalDuration(ranges) in availability.mjs for a scheduling library. Ranges are [start, end] pairs representing half-open intervals. Require an array of exactly two-element arrays with finite safe-integer endpoints and start < end; reject invalid input. Negative endpoints are valid. Return newly allocated pairs sorted by start, merging all overlapping or touching intervals. Do not mutate or alias the input, including when a single range is supplied. Empty input returns []. totalDuration must return the length of the union, not double-count overlaps, and must apply the same validation. Keep the public exports.

Implement both functions and run node --test test.mjs. Preserve supplied tests. Do not add dependencies. Work only in this repository and leave changes uncommitted.
